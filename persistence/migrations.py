from __future__ import annotations


def migrate(data: dict, from_version: int) -> dict:
    """Migrate a schema dict from *from_version* to the current version.

    Stub — no migrations needed yet. Extend with if-chains as schema_version
    increments in future releases.
    """
    return data
